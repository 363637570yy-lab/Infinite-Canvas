# 画布出片提示词适配方案

日期：2026-08-17  
范围：Infinite Canvas 导演本 → 各视频模型合同  
不改：既有画布编辑器与直接生成链路、H3 网关换模 / 方案档位 / LoRA 选择  
备注：网关主机、部署与容器事实不写入本仓库，见 `.codex-project/` 与全局服务器档案。

## 1. 要解决什么

画布现在写的是中文分镜（`镜头1`、`皇帝@图1`、`台词：`）。网关原样送给模型。

多图任务里，参考图已经进了 Ref2VA 槽位，但提示词没有官方绑定。H3 看到的是「两张图 + 一段中文」，不是「图1 = 这个皇帝」。FL2VA 还会复用同一段多图剧本，词里的 `@图2` 根本没有对应文件。

这不是 H3 网关换模或方案的问题，是出片前没有「目标适配」。

## 2. 原则

总原则：本功能是**纯增量**——只加转换按钮、转换端点和派生工作台；除第 10 节标注的「H3 槽位小修」外，不改任何既有生成行为。

1. 画布继续当导演本编辑器，不强迫用户写官方英文。
2. 出片前按**用户选定的目标**转换，与工作台当前站点 / 模型解耦；转换结果可预览、可改、可再生成。
3. 五个出口是五个适配器，不是五个编辑器。
4. 逻辑管不会错的结构；AI 管必须写成句子的正文。
5. 每个目标一份规范 skill，不是一个「帮我写提示词」的泛提示。
6. 校验失败不发出去。

## 3. 画布要改的硬逻辑（不靠 AI）

这些现在不做，后面转换也会绑错。除「图槽」一条是对既有 H3 提交行为的小修外，其余全部只落在转换 / 派生路径上，不碰直接生成。

| 项 | 现在 | 要改成 |
|---|---|---|
| 图槽 | H3 把第一张无角色图自动吃进首帧槽，不读「全能参考」勾选 | 读既有勾选分槽：全能参考 → `ref_image_0..N`，首尾帧 → first/last。UI 不变，**这是全方案唯一改既有行为的点** |
| 图号 | 提示词装饰 | 转换时 `@图N` / `@文件名` 与 MEDIA 实名对齐，对不上警告 |
| 模式 | 同一段词可送给 FL2VA 和 Ref2VA | 转换按目标分模式，非法组合在转换时拒绝或警告；直接生成不拦 |
| 发出去的词 | 导演本原文 | 源工作台直接生成仍发原文（现状保留）；转换出片发派生工作台的终稿 |

非法组合示例（都在转换时拦截，不影响直接生成）：

- 首尾帧目标带角色参考图（该目标只收首帧 / 尾帧）
- 多参目标一张参考图都没有
- 词里写了 `@图2` 但只传了 1 张图
- 15 秒词里写了 8 个镜头且没有时间码（转换后必须补齐或降镜）

## 4. 中间稿（Canvas IR）

导演本先抽成结构化中间稿，再给各目标适配。中间稿是唯一事实源。

```text
duration_s          5 | 10 | 15
mode                t2v | fl2va | ref2va
style               用户写的风格词（柔光 / 电影感…）
shots[]
  index             1..N
  at_s              可选；没有则转换时按时长均分或 AI 估，逻辑再夹紧
  action            画面与动作（中文）
  camera            运镜口语（拉远 / 环绕 / 推进）
  dialogue[]        speaker, text（原文，不翻译）
subjects[]
  id                皇帝 / 甄嬛
  image             图1 | 图2 | null
  notes             服饰、站位
images[]
  slot              图1…
  role              first | last | reference | unknown
  file              实际上传文件
sound               用户若写了环境声 / 配乐，原样留下
```

抽取规则：

- `@图N`、`@文件名.png`（现网导演本实际写法，如 `@阿川_ref.png`）都要与 MEDIA 清单实名对齐，对不上就标红，不猜测；词里引用了未挂载的图必须警告。
- `台词：角色：「…」` 抽成 dialogue，正文一字不改。
- `镜头N` 抽成 shots。没有时间码先留空。
- 抽不出中间稿（纯一句形容词）也可以转，但多参 / 首尾帧要警告「绑定不完整」。

中间稿简表（镜头、人物、图号）随转换结果一并展示在派生工作台上；一次点击完成转换，不加中途确认步骤。

## 5. 五个目标

| ID | 名称 | 图怎么送 | 词的合同 |
|---|---|---|---|
| `h3-t2v` | H3 文生视频 | 无图 | 英文三字段：`integrated_multimodal_description` + `overall_soundscape` + `non_diegetic_music` |
| `h3-fl2va` | H3 首尾帧 | 首帧必填，尾帧可选 | 第一行对齐指令，空一行，再写三字段；尽量单镜头，写从首帧走到尾帧的路径 |
| `h3-ref2va` | H3 多参 | 1–9 张参考图 | 六段：`subject_definitions` / `summary` / `retention_analysis` / `detailed_description` / `overall_soundscape` / `non_diegetic_music`；`<Subject N>` 绑 `<Picture N>` |
| `seedance-2.0` | Seedance 2.0 | `@Image N` 对应上传槽 | 短英文：主体 / 动作 / 场景 / 风格 / 运镜 / 声音；图写成 `@Image 1` |
| `seedance-2.5` | Seedance 2.5 | 同上，首尾帧分行声明 | 官方六段公式；首尾帧必须分行写 `@Image 1 is the first frame`，禁止写成 “@Images 1 and 2 are the first and last frames” |

H3 镜头与对白（三档共用）：

- `[Shot 1]` 不带时间；之后 `[Shot N] At MM:SS.mmm, the camera cuts to...`
- 运镜写成句子：`The camera pulls out with small amplitude at slow speed`
- 说话人 `(S1)`，台词 `<d>[Chinese] 原文</d>`
- 没有配乐写 `non_diegetic_music: N/A`
- 正文英文；台词、字幕、牌匾不翻译

画布 `@图1` 更像 Seedance，不像 H3。同一段中间稿：

- H3 → `<Picture 1>` / `<Subject 1>`
- Seedance → `@Image 1`

## 6. 逻辑和 AI 怎么分

```text
导演本
  → 逻辑：抽中间稿、对齐图槽、拒非法模式
  → AI：按「当前目标 skill」填正文
  → 逻辑：校验字段 / 图号 / 时间码 / 台词未改
  → 预览 → 用户可改 → 发出
```

逻辑做：

- 图槽、模式、时长
- 镜头序号、时间码夹在 `[0, duration]`，且严格递增
- 包字段名、补 `N/A`
- 校验必填段、禁止发明新图号
- 对比中间稿 dialogue，`<d>` 里必须是原文

AI 做：

- 中文画面译成英文描写
- 口语运镜改成官方相机句
- 分配 `(S1)` `(S2)`
- 补环境声；用户没写配乐则 `N/A`
- FL2VA 补「首帧状态 → 中间动作 → 尾帧落点」
- 多参写 `subject_definitions` / `retention_analysis`

禁止纯逻辑硬拼英文（质量不够），也禁止纯 AI 一次生成（会漏字段、改台词、弄乱图号）。

## 7. Skill 怎么写

一个目标一份 skill，不要合成「万能改写」。

每份必须有：

1. **输入**：只收中间稿 JSON + `duration_s` + 图清单，不收闲聊。
2. **输出 schema**：字段名、顺序、语言、标签语法。
3. **禁令**：不翻译 `<d>`；不新增 `@图` / `<Picture>` 序号；不删 `overall_soundscape`；不把多参写成 FL2VA 对齐行。
4. **正反例句**：至少 1 个官方合格样例 + 1 个画布病句（就是现网那条杏花御花园）。
5. **自检清单**：模型先对照清单，逻辑再跑一遍。

建议文件（画布仓库，不进 H3 网关）：

```text
prompts/video-targets/
  canvas-ir.md              中间稿字段
  h3-t2v.md
  h3-fl2va.md
  h3-ref2va.md
  seedance-2.0.md
  seedance-2.5.md
```

目录不叫 `skills/`，避免与 `.agents/skills` 的代理 Skill 撞名。

H3 三档可抽公共「镜头 / 运镜 / 对白」小节，但出口 skill 仍要分开，避免文生带上多参六段。

校验失败：自动重跑 1 次（把失败项写进提示）；再失败则展示原文 + 错误，不提交网关。

## 8. 交互（派生工作台式）

1. 用户在视频工作台写 / 接入导演本、挂图，可照常直接点「视频生成」（现状不变）。
2. 工作台上另有一组固定按钮：seedance2.0 / seedance2.5 / H3多参 / H3首尾帧 / H3文生 提示词，与当前站点、模型无关。
3. 点击后执行转换：逻辑抽中间稿 → AI 按目标 skill 写正文 → 逻辑校验；AI 用画布已配置的文本模型通道。
4. 转换成功后在画布上**派生一个新的视频生成工作台**：提示词填入转换结果（可改），MEDIA、时长、比例从源工作台复制（复制不共享），模型默认选中与目标配套的那个（可改），保留「源 → 派生」连线。校验警告标在派生工作台上。
5. 在派生工作台照常生成；导演本留在源工作台，转换稿随画布落盘，刷新不丢。
6. 转换失败（AI 超时 / 校验两次不过）不派生节点，在源工作台标红原因。
7. 同一源工作台对同一目标重复转换时，询问「更新已派生的工作台还是新建」，避免节点堆积。

不要静默替换用户看到的分镜。导演本和目标词分别存在两个工作台节点上。

## 9. 和 H3 网关的边界

| 在画布 | 在 H3 网关 |
|---|---|
| 抽中间稿、转换、预览、校验 | 收已经合格的 prompt |
| 按模式选图槽 | 继续按 `mode` 收 `first_frame` / `last_frame` / `ref_image_N` |
| 选 H3 文生 / 首尾帧 / 多参 | 已有 `fl2va` / `ref2va`；文生=无图 FL2VA 或后续补 T2VA |

H3 网关**第一期不做 rewriter**。网关继续当合同执行器：尺寸、时长、方案、图槽。词的责任在画布。

若以后有非画布客户端，再把同一套 skill + 校验抽成独立服务。现在不要提前搬到网关。

## 10. 分期

### 第一期

1. 转换端点 + 中间稿抽取（规则为主，AI 只补分镜切分）+ 校验框架，落独立后端模块，不进 `main.py` 主体。
2. 两份 skill：`seedance-2.0`、`seedance-2.5`。纯改词即可用：codelba 把图按顺序发 `image_refs`，与 `@Image N` 天然对齐，画布现状可直接出片。
3. 工作台转换按钮 + 派生工作台。
4. H3 槽位小修（后端不可见，UI 不变，全方案唯一改既有行为的点）：勾「全能参考」→ 全部图进 `ref_image_N`，不再自动占用首帧槽；勾「首尾帧」→ first/last。
5. 两份 skill：`h3-ref2va`、`h3-fl2va`。

验收一（Seedance）：用现网 ACT01 导演本（`@文件名` 引用 + 9 宫格分镜参考图）转 `seedance-2.5`，`@Image N` 与 MEDIA 实名对齐；词里引用了未挂载的图必须出警告。

验收二（H3）：用现网那条杏花 / 皇帝@图1 / 甄嬛@图2 / 四段对白。

- 多参：词里出现 `<Subject 1>…<Picture 1>`、`<Subject 2>…<Picture 2>`，四句台词原文都在 `<d>` 里，有 `overall_soundscape`。
- 首尾帧：有对齐行；若只传了皇帝图，词里不得再要求甄嬛@图2 上场，或明确标「缺图，已降级」。
- 同一导演本切到 FL2VA 不得再把 2 张参考图塞进 Ref2VA 槽。

### 第二期

- `h3-t2v`
- 转换结果写入画布节点历史，方便回看哪版词出的片

### 不做

- 在既有直接生成链路上加拦截、强制校验或任何行为变更（H3 槽位小修除外）
- 让用户直接在画布写官方英文当唯一输入
- 一个 prompt 吃五个目标
- 在 H3 网关 `h3_pin_unet` 或方案页里改词
- 用转换代替选对模型（多参必须走 Ref2VA 底模）

## 11. 现网病句（第一期必测）

三条任务（`video_cc668c38` / `ad39ba4a` / `cd862bb1`）用了同一段 692 字中文：

- `镜头1`…`镜头4`，无时间码
- `皇帝@图1`、`甄嬛@图2`
- `台词：甄嬛：「臣妾参见皇上。」` 等
- 结尾「柔光摄影 / 8K超高清」
- 无环境声、无配乐字段

转换后应分别得到合格的 `h3-ref2va` 与（在只有首帧时）降级或拒绝的 `h3-fl2va`；转换出片链路不得把这段原文原样发给模型。直接生成路径不受影响，仍由用户自担。

## 12. 未决

1. AI 用画布现有模型还是单独小模型。要求：能稳定守 schema，延迟控制在点生成可接受范围内（建议转换 < 8s）。
2. 15 秒 4 镜是否自动均分时间，还是让 AI 按对白估点、逻辑只做夹紧。
3. 用户改了转换词之后，是否回写中间稿。建议不回写，避免污染导演本。
4. Seedance 与 H3 的「多参」图数上限不同，画布选目标时就要截断或拒，不要转到一半再失败。
